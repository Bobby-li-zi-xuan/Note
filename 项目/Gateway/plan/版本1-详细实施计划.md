# 版本 1：详细实施计划（含关键代码段）

> 配套文档：《版本1-统一接入与基础网关（学习版）》讲“是什么、为什么”，本文档讲“怎么做”。
> 技术基线：Java 21、Spring Boot 3.2、Spring MVC + Java 21 虚拟线程（阻塞式）、Maven 多模块（ai-gateway / mock-model-server）。
> 本文档的关键代码段是**实现参考**，允许按你的编码风格调整；但接口契约（配置结构、端点、错误体）请保持一致。
>
> ⚠️ 2026-08-05：项目已从 WebFlux（响应式）全盘改为 Spring MVC + Java 21 虚拟线程（阻塞式）。本文代码段已同步为阻塞式写法，逻辑与接口契约不变。

---

## 0. 手敲清单与实施分工（先读）

版本 1 的代码分两类：**必须你手敲默写的核心（H1~H5，约 250 行）**与 **Codex 已实现的脚手架**。

### 必须手敲默写（H1~H5）

| 编号 | 手敲内容 | 位置 | 为什么必须手敲 |
|---|---|---|---|
| H1 | 加权随机算法 WeightedRandomPicker | 第 8 节 S6 | 面试白板高频题，算法核心 |
| H2 | 路由主流程：健康过滤 + 加权选择 + 失败切换 | 第 9 节 S7 | 网关灵魂，失败切换与容错处理 |
| H3 | 流式 SSE 解析（stream + parseSseLine） | 第 10 节 S8 后半 | AI 网关差异化能力 |
| H4 | 健康检查状态转换（mark / isHealthy） | 第 13 节 S13 | 状态机与防御式设计 |
| H5 | 模型注册启动校验（ModelRegistry.init 校验规则） | 第 7.3 节 S5b | 启动期暴露错误的工程思想 |

手敲方法：先合上文档自己写 → 写不出来再看代码段 → 看完再自己重写一遍，直到能默写。

手敲类路径：H1 `ai-gateway/src/main/java/com/aigateway/core/service/WeightedRandomPicker.java`；H2 `ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java`；H3 `ai-gateway/src/main/java/com/aigateway/execution/connector/OpenAIConnector.java`；H4 `ai-gateway/src/main/java/com/aigateway/state/health/HealthChecker.java`；H5 `ai-gateway/src/main/java/com/aigateway/state/registry/ModelRegistry.java`。

### Codex 已实现（脚手架，不需要手敲）

pom 依赖、application.yaml / model.yml、DTO（ChatRequest/ChatCompletion/ChatChunk/ModelsResponse）、Channel/ModelInstance、GatewayProperties、SecretResolver、HttpClientFactory（新增）、RequestIdFilter、GlobalExceptionHandler、ChatCompletionController/ModelController、GatewayMetrics、OpenAIConnector.complete()（非流式）、HealthChecker 探活调度、mock 流式与健康开关。

代码中手敲部分留 `TODO H1~H5` 标记与说明注释，提示你该写哪一段；未完成前调用对应功能会直接报错。

---

## 1. 目标与范围

版本 1 完成后，系统支持：

- `POST /v1/chat/completions`：非流式与流式（SSE）两种调用。
- `GET /v1/models`：列出已注册的模型别名。
- 配置驱动：`channels`（渠道）+ `models`（别名 → 候选），启动时完整校验。
- 加权随机路由 + 健康过滤 + 失败切换（最多尝试 N 个候选）。
- mock 服务可动态调延迟、故障率、健康状态。
- 全链路请求 ID、结构化日志、基础指标。

**非目标**：插件框架（V2）、重试退避/熔断/冷却（V3）、令牌/限流/计量（V4）、热更新（V5）。

---

## 2. 与现有代码的关系（先盘点，再动手）

| 现有文件 | 现状 | 处理方式 |
|---|---|---|
| `ModelConfig`（gateway.models） | 只有实例列表，无渠道/权重/别名 | 已重构为 `GatewayProperties`（channels + models + health） |
| `ModelRegistry` | 实例 Map，按 ID 查询 | 已改造为“别名 → 候选列表”，校验规则留 H5 手敲 |
| `HealthChecker` | 5 秒探活，UP/DEGRADED 两态 | 已改为“连续失败阈值 + 快照查询”，状态转换留 H4 手敲 |
| `OpenAIConnector` | 非流式（阻塞式 HttpClient） | 已改为类型化 DTO + 非流式实现，流式留 H3 手敲 |
| `ChatCompletionController` | `Map` 出入参、无流式 | 已重构为类型化 DTO + 流式 + 统一错误 |
| `HttpClientFactory` | 已有连接池与超时 | 保留 |
| mock `ChatController` | 流式用了 `@PatchMapping`（不标准） | 已改为 `POST + params=stream=true`（`SseEmitter` 自动按 `data:` 输出） |
| mock `HealthController` | 永远返回 UP | 已增加健康开关（Behavior API 控制） |

已删除的旧代码：决策引擎相关类（DecisionService、CandidateFilter、SimpleScorer、RoutingDecision 等）、ModelConfig、ModelRepository（V2 再按新设计重建）。
> 路径前缀：`ai-gateway/src/main/java/com/aigateway/`；mock 相关类在 `mock-model-server/src/main/java/com/aigateway/mock/` 下。

---

## 3. 工程结构与新增文件

```
ai-gateway/src/main/java/com/aigateway/
├── GatewayApplication.java                    （保留）
├── api/
│   ├── controller/ChatCompletionController.java（已实现）
│   ├── controller/ModelController.java         （已实现：/v1/models）
│   ├── dto/ChatRequest.java                    （已实现：record）
│   ├── dto/ChatCompletion.java                 （已实现：record）
│   ├── dto/ChatChunk.java                      （已实现：record）
│   └── error/GlobalExceptionHandler.java       （已实现）
├── core/
│   ├── domain/model/Channel.java               （已实现：record）
│   ├── domain/model/ModelInstance.java         （已实现：record）
│   ├── exception/GatewayException.java         （已实现：status + type）
│   └── service/ChatGatewayService.java         （🖊 H2 手敲：TODO 桩）
├── infra/
│   ├── config/GatewayProperties.java           （已实现）
│   ├── config/SecretResolver.java              （已实现：env: 解析）
│   ├── http/HttpClientFactory.java              （保留）
│   └── web/RequestIdFilter.java                （已实现）
├── state/
│   ├── health/HealthChecker.java               （探活已实现；🖊 H4 手敲：mark/isHealthy）
│   └── registry/ModelRegistry.java             （结构已实现；🖊 H5 手敲：校验规则）
├── execution/
│   └── connector/OpenAIConnector.java          （complete 已实现；🖊 H3 手敲：stream/parseSseLine）
└── observability/GatewayMetrics.java           （已实现：Micrometer）

mock-model-server/src/main/java/com/aigateway/mock/
├── controller/ChatController.java              （已实现：标准流式）
├── controller/BehaviorController.java          （已实现：health 开关）
├── controller/HealthController.java            （已实现：尊重健康开关）
└── model/ModelRegistry.java                    （已实现：healthy 字段）
```

---

## 4. 依赖（pom.xml）
> 路径：`ai-gateway/pom.xml`

`ai-gateway/pom.xml` 使用 `spring-boot-starter-web`（Spring MVC）并开启虚拟线程，已加入（代码段 S1）：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```

说明：actuator + prometheus 用于 `/actuator/prometheus` 指标端点；虚拟线程通过 `spring.threads.virtual.enabled=true` 开启。

---

## 5. 配置设计（关键代码段 S2，已实现）

### 5.1 `application.yaml`（保留 server 配置）
> 路径：`ai-gateway/src/main/resources/application.yaml`

```yaml
server:
  port: 8080

spring:
  config:
    import:
      - classpath:model.yml
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
```

### 5.2 `model.yml`（核心：渠道 + 模型候选）
> 路径：`ai-gateway/src/main/resources/model.yml`

```yaml
gateway:
  health:
    intervalSeconds: 5
    timeoutMs: 2000
    consecutiveFailures: 3
  channels:
    - id: mock-a
      provider: openai-compatible
      baseUrl: http://localhost:8001
      credentialsRef: ""          # 空表示无需密钥（mock）；真实上游写 env:OPENAI_API_KEY
      weight: 10
    - id: mock-b
      provider: openai-compatible
      baseUrl: http://localhost:8002
      credentialsRef: ""
      weight: 5
  models:
    - alias: qwen
      candidates:
        - channelId: mock-a
          model: qwen-large
          weight: 8
          capability:
            contextLength: 128000
            multimodal: false
            toolCallSupport: true
        - channelId: mock-b
          model: qwen-small
          weight: 2
          capability:
            contextLength: 32000
            multimodal: false
            toolCallSupport: false
    - alias: deepseek-code
      candidates:
        - channelId: mock-b
          model: deepseek-code
          weight: 1
          capability:
            contextLength: 64000
            multimodal: false
            toolCallSupport: true
```

要点：

- `alias` = 客户端传的模型名（对应 LiteLLM 的 `model_name`）；
- `candidates` = 候选实例列表（对应 LiteLLM 的同名多条记录）；
- 权重是相对比例：alias `qwen` 下 mock-a 占 8/10，mock-b 占 2/10。

---

## 6. 领域模型（关键代码段 S3，已实现）

```java
// 路径：ai-gateway/src/main/java/com/aigateway/core/domain/model/Channel.java
public record Channel(String id, String provider, String baseUrl,
                      String credentialsRef, int weight) {
    public boolean needAuth() {
        return credentialsRef != null && !credentialsRef.isBlank();
    }
}

// 路径：ai-gateway/src/main/java/com/aigateway/core/domain/model/ModelInstance.java
public record ModelInstance(
        String instanceId,       // channelId + ":" + model，唯一
        String alias,            // 对外模型名
        String channelId,
        String model,            // 上游模型名
        int weight,
        Capability capability
) {}

// 路径：ai-gateway/src/main/java/com/aigateway/api/dto/ChatRequest.java（只保留 V1 需要的字段）
public record ChatRequest(
        String model,
        List<Message> messages,
        Boolean stream,
        Double temperature,
        Integer maxTokens
) {
    public record Message(String role, String content) {}

    public boolean streaming() {
        return Boolean.TRUE.equals(stream);
    }
}

// 路径：ai-gateway/src/main/java/com/aigateway/api/dto/ChatCompletion.java（OpenAI 非流式响应）
public record ChatCompletion(
        String id,
        String object,
        long created,
        String model,
        List<Choice> choices,
        Usage usage
) {
    public record Choice(int index, Message message,
                         @JsonProperty("finish_reason") String finishReason) {
        public record Message(String role, String content) {}
    }
    public record Usage(@JsonProperty("prompt_tokens") int promptTokens,
                        @JsonProperty("completion_tokens") int completionTokens,
                        @JsonProperty("total_tokens") int totalTokens) {}
}

// 路径：ai-gateway/src/main/java/com/aigateway/api/dto/ChatChunk.java（OpenAI 流式分片）
public record ChatChunk(
        String id,
        String object,
        long created,
        String model,
        List<ChunkChoice> choices
) {
    public record ChunkChoice(int index, Delta delta,
                              @JsonProperty("finish_reason") String finishReason) {
        public record Delta(String content) {}
    }
}
```

注意：Jackson 默认不做下划线转换，DTO 已用 `@JsonProperty` 显式映射 `finish_reason` / `prompt_tokens` 等字段；以后新增字段记得加注解。

---

## 7. 配置绑定与启动校验（关键代码段 S4、S5）
> 路径：`ai-gateway/src/main/java/com/aigateway/infra/config/GatewayProperties.java`、`ai-gateway/src/main/java/com/aigateway/infra/config/SecretResolver.java`、`ai-gateway/src/main/java/com/aigateway/state/registry/ModelRegistry.java`

> 🖊 **手敲 H5**：仅 `ModelRegistry.init()` 中的校验规则手敲；配置绑定（S4）与密钥解析（S5a）已由 Codex 实现。

### 7.1 `GatewayProperties`（代码段 S4，已实现）
> 路径：`ai-gateway/src/main/java/com/aigateway/infra/config/GatewayProperties.java`

```java
@Configuration
@ConfigurationProperties(prefix = "gateway")
@Data
public class GatewayProperties {

    private Health health = new Health();
    private List<ChannelDef> channels = new ArrayList<>();
    private List<ModelDef> models = new ArrayList<>();

    @Data
    public static class Health {
        private long intervalSeconds = 5;
        private long timeoutMs = 2000;
        private int consecutiveFailures = 3;
    }

    @Data
    public static class ChannelDef {
        private String id;
        private String provider;
        private String baseUrl;
        private String credentialsRef = "";
        private int weight = 1;
    }

    @Data
    public static class ModelDef {
        private String alias;
        private List<CandidateDef> candidates = new ArrayList<>();
    }

    @Data
    public static class CandidateDef {
        private String channelId;
        private String model;
        private int weight = 1;
        private Capability capability;
    }
}
```

### 7.2 密钥解析（代码段 S5a，已实现）
> 路径：`ai-gateway/src/main/java/com/aigateway/infra/config/SecretResolver.java`

```java
@Component
public class SecretResolver {
    /** 支持 "env:XXX" 或空字符串；启动时解析，运行期只保存值 */
    public Optional<String> resolve(String credentialsRef) {
        if (credentialsRef == null || credentialsRef.isBlank()) {
            return Optional.empty();
        }
        if (credentialsRef.startsWith("env:")) {
            String var = credentialsRef.substring(4);
            return Optional.ofNullable(System.getenv(var));
        }
        throw new GatewayException(500, "invalid_config",
                "credentialsRef 只支持 env: 引用: " + credentialsRef);
    }
}
```

### 7.3 `ModelRegistry` 构建与校验（代码段 S5b）
> 路径：`ai-gateway/src/main/java/com/aigateway/state/registry/ModelRegistry.java`

结构（渠道表、别名→候选映射、查询方法）已实现；**校验规则请你手敲**。代码中的 TODO 如下：

```java
private void init(GatewayProperties props) {
    // ============================================================
    // TODO H5（手敲）：启动校验
    // 1) 渠道：credentialsRef 必须可解析（secretResolver.resolve 抛错即启动失败）
    // 2) 模型：alias 非空；候选非空；权重 > 0；候选引用的渠道必须存在
    // 3) 重复 alias 应拒绝
    // 校验失败统一抛 GatewayException(500, "invalid_config", 可读信息)
    // ============================================================

    for (GatewayProperties.ChannelDef def : props.getChannels()) {
        channels.put(def.getId(), new Channel(
                def.getId(), def.getProvider(), def.getBaseUrl(),
                def.getCredentialsRef(), def.getWeight()));
    }

    for (GatewayProperties.ModelDef def : props.getModels()) {
        List<ModelInstance> instances = new ArrayList<>();
        for (GatewayProperties.CandidateDef c : def.getCandidates()) {
            Channel channel = channels.get(c.getChannelId());
            instances.add(new ModelInstance(
                    c.getChannelId() + ":" + c.getModel(),
                    def.getAlias(), c.getChannelId(), c.getModel(),
                    c.getWeight(), c.getCapability()));
        }
        byAlias.put(def.getAlias(), List.copyOf(instances));
    }
}
```

手敲验收：把 `model.yml` 改坏（如引用不存在的渠道、权重写 0）后启动，必须在启动阶段报错而不是运行中报错。

---

## 8. 加权随机选择（关键代码段 S6）
> 路径：`ai-gateway/src/main/java/com/aigateway/core/service/WeightedRandomPicker.java`

> 🖊 **手敲 H1**：本节全部手敲（Codex 未提供该文件）。

这是 V1 的核心算法：权重累加区间 + 随机落点。

```java
package com.aigateway.core.service;

import com.aigateway.core.domain.model.ModelInstance;

import java.util.List;
import java.util.Optional;
import java.util.concurrent.ThreadLocalRandom;

/**
 * 加权随机选择器（不可变，构建后线程安全）。
 * 算法：把候选按权重在数轴上依次累加，随机数落在哪个区间就选谁。
 */
public final class WeightedRandomPicker {

    private final List<ModelInstance> candidates;
    private final int[] cumulative;   // cumulative[i] = 前 i+1 个候选的权重和
    private final int totalWeight;

    public WeightedRandomPicker(List<ModelInstance> candidates) {
        this.candidates = List.copyOf(candidates);
        this.cumulative = new int[candidates.size()];
        int sum = 0;
        for (int i = 0; i < candidates.size(); i++) {
            sum += candidates.get(i).weight();
            cumulative[i] = sum;
        }
        this.totalWeight = sum;
    }

    public boolean isEmpty() {
        return totalWeight <= 0;
    }

    public Optional<ModelInstance> pick() {
        if (isEmpty()) {
            return Optional.empty();
        }
        int r = ThreadLocalRandom.current().nextInt(totalWeight); // [0, totalWeight)
        for (int i = 0; i < cumulative.length; i++) {
            if (r < cumulative[i]) {
                return Optional.of(candidates.get(i));
            }
        }
        return Optional.of(candidates.get(candidates.size() - 1)); // 防御
    }
}
```

边界行为（写单测时逐条验证）：

- 权重 8:2 → 选中概率约 80% / 20%；
- 单个候选、权重任意 → 永远选中；
- 空列表 → `isEmpty()` 为 true；
- 候选顺序不影响正确性（只影响区间划分）。

---

## 9. 主流程：路由 + 转发 + 失败切换（关键代码段 S7）
> 路径：`ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java`

> 🖊 **手敲 H2**：本节全部手敲（阻塞式版）；代码中 `ChatGatewayService` 为 TODO 桩。

```java
package com.aigateway.core.service;

@Service
public class ChatGatewayService {

    private final ModelRegistry registry;
    private final HealthChecker healthChecker;
    private final OpenAIConnector connector;
    private final GatewayMetrics metrics;

    /** 非流式：失败时换下一个候选，最多尝试 maxAttempts 个（阻塞式） */
    public ChatCompletion complete(ChatRequest request, String requestId) {
        List<ModelInstance> candidates = healthyCandidates(request.model());
        if (candidates.isEmpty()) {
            throw new GatewayException(503, "no_available_model",
                    "模型[" + request.model() + "] 当前没有可用实例");
        }
        int attempt = 0;
        while (attempt < maxAttempts() && !candidates.isEmpty()) {
            ModelInstance chosen = pick(request, requestId, candidates);
            try {
                ChatCompletion completion = connector.complete(chosen, request);
                metrics.success(request.model(), chosen.instanceId());
                return completion;
            } catch (Exception e) {
                metrics.failure(request.model(), chosen.instanceId());
                log(requestId, "候选失败，尝试下一个", chosen.instanceId() + " - " + e.getMessage());
                candidates = remaining(candidates, chosen);
                attempt++;
            }
        }
        throw new GatewayException(502, "upstream_failed", "所有候选均失败");
    }

    private ModelInstance pick(ChatRequest request, String requestId, List<ModelInstance> candidates) {
        ModelInstance chosen = new WeightedRandomPicker(candidates).pick()
                .orElseThrow(() -> new GatewayException(503, "no_available_model", "候选为空"));
        log(requestId, "路由选择", chosen.instanceId());
        return chosen;
    }

    /** 流式：首字节前失败才切换候选；已经开始输出则直接中断（V3 安全重试边界） */
    public void stream(ChatRequest request, String requestId, Consumer<ChatChunk> consumer) {
        List<ModelInstance> candidates = healthyCandidates(request.model());
        if (candidates.isEmpty()) {
            throw new GatewayException(503, "no_available_model",
                    "模型[" + request.model() + "] 当前没有可用实例");
        }
        int attempt = 0;
        while (attempt < maxAttempts() && !candidates.isEmpty()) {
            ModelInstance chosen = pick(request, requestId, candidates);
            boolean[] started = {false};
            try {
                connector.stream(chosen, request, chunk -> {
                    metrics.streamChunk(request.model(), chosen.instanceId());
                    started[0] = true;
                    consumer.accept(chunk);
                });
                return;
            } catch (Exception e) {
                if (started[0]) {
                    throw new GatewayException(502, "upstream_failed",
                            "流式输出中断: " + e.getMessage(), e);
                }
                metrics.failure(request.model(), chosen.instanceId());
                log(requestId, "流式候选失败(首字节前)，尝试下一个",
                        chosen.instanceId() + " - " + e.getMessage());
                candidates = remaining(candidates, chosen);
                attempt++;
            }
        }
        throw new GatewayException(502, "upstream_failed", "所有候选均失败");
    }

    private List<ModelInstance> remaining(List<ModelInstance> candidates, ModelInstance chosen) {
        return candidates.stream()
                .filter(c -> !c.instanceId().equals(chosen.instanceId()))
                .toList();
    }

    private List<ModelInstance> healthyCandidates(String alias) {
        return registry.findByAlias(alias).stream()
                .filter(inst -> healthChecker.isHealthy(inst.channelId()))
                .toList();
    }

    private int maxAttempts() {
        return 2; // 可配置化（V5 热更新），V1 先用常量
    }
}
```

设计要点：

- 每次尝试用“剩余候选”重新做加权随机（而不是固定顺序），保证失败切换后权重仍然生效；
- 非流式切换 = 简单换候选（V1），V3 再引入重试退避/熔断；
- 流式目前也只处理“首字节前”的失败——这个边界从 V1 就写清楚，V3 直接沿用；
- `log(requestId, event, detail)` 用 System.out 或 Logger 均可，格式建议 `[requestId=xxx] event=xxx detail=xxx`。

---

## 10. Connector：非流式 + 流式 SSE（关键代码段 S8）
> 路径：`ai-gateway/src/main/java/com/aigateway/execution/connector/OpenAIConnector.java`

> 🖊 **手敲 H3**：仅 `stream()` 与 `parseSseLine()` 手敲；`complete()` 已实现。

```java
package com.aigateway.execution.connector;

@Component
public class OpenAIConnector {

    private final HttpClient httpClient;
    private final ModelRegistry registry;
    private final SecretResolver secretResolver;
    private final ObjectMapper objectMapper;

    public OpenAIConnector(HttpClient httpClient,
                           ModelRegistry registry,
                           SecretResolver secretResolver,
                           ObjectMapper objectMapper) {
        this.httpClient = httpClient;
        this.registry = registry;
        this.secretResolver = secretResolver;
        this.objectMapper = objectMapper;
    }

    public ChatCompletion complete(ModelInstance instance, ChatRequest request) {
        HttpRequest.Builder builder = HttpRequest.newBuilder()
                .uri(URI.create(channelBaseUrl(instance) + "/v1/chat/completions"))
                .header("Content-Type", "application/json")
                .header("Accept", "application/json")
                .timeout(Duration.ofSeconds(30))
                .POST(HttpRequest.BodyPublishers.ofString(writeJson(request)));
        applyAuth(builder, instance);
        try {
            HttpResponse<String> response = httpClient.send(builder.build(),
                    HttpResponse.BodyHandlers.ofString());
            if (response.statusCode() / 100 != 2) {
                throw new GatewayException(502, "upstream_error",
                        "上游返回 " + response.statusCode() + ": " + response.body());
            }
            return objectMapper.readValue(response.body(), ChatCompletion.class);
        } catch (GatewayException e) {
            throw e;
        } catch (IOException | InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new GatewayException(502, "upstream_failed", "调用上游失败: " + e.getMessage(), e);
        }
    }

    public void stream(ModelInstance instance, ChatRequest request, Consumer<ChatChunk> consumer) {
        HttpRequest.Builder builder = HttpRequest.newBuilder()
                .uri(URI.create(channelBaseUrl(instance) + "/v1/chat/completions"))
                .header("Content-Type", "application/json")
                .header("Accept", "text/event-stream")
                .timeout(Duration.ofSeconds(30))
                .POST(HttpRequest.BodyPublishers.ofString(writeJson(request.withStream(true))));
        applyAuth(builder, instance);
        try {
            HttpResponse<InputStream> response = httpClient.send(builder.build(),
                    HttpResponse.BodyHandlers.ofInputStream());
            if (response.statusCode() / 100 != 2) {
                String body = new String(response.body().readAllBytes(), StandardCharsets.UTF_8);
                throw new GatewayException(502, "upstream_error",
                        "上游返回 " + response.statusCode() + ": " + body);
            }
            try (BufferedReader reader = new BufferedReader(
                    new InputStreamReader(response.body(), StandardCharsets.UTF_8))) {
                String line;
                while ((line = reader.readLine()) != null) {
                    ChatChunk chunk = parseSseLine(line);
                    if (chunk != null) {
                        consumer.accept(chunk);
                    }
                }
            }
        } catch (GatewayException e) {
            throw e;
        } catch (IOException | InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new GatewayException(502, "upstream_failed", "流式调用上游失败: " + e.getMessage(), e);
        }
    }

    private void applyAuth(HttpRequest.Builder builder, ModelInstance instance) {
        registry.findChannel(instance.channelId())
                .filter(Channel::needAuth)
                .ifPresent(ch -> secretResolver.resolve(ch.credentialsRef())
                        .ifPresent(token -> builder.header("Authorization", "Bearer " + token)));
    }

    private String writeJson(Object body) {
        try {
            return objectMapper.writeValueAsString(body);
        } catch (IOException e) {
            throw new GatewayException(500, "internal_error", "请求序列化失败", e);
        }
    }

    private String channelBaseUrl(ModelInstance instance) {
        return registry.findChannel(instance.channelId())
                .map(Channel::baseUrl)
                .orElseThrow(() -> new GatewayException(500, "invalid_config",
                        "渠道不存在: " + instance.channelId()));
    }

    /** 解析 "data: {...}" / "data: [DONE]"；非 data 行返回 null */
    private ChatChunk parseSseLine(String line) {
        String trimmed = line.trim();
        if (!trimmed.startsWith("data:")) {
            return null;
        }
        String payload = trimmed.substring(5).trim();
        if ("[DONE]".equals(payload)) {
            return null;
        }
        try {
            return objectMapper.readValue(payload, ChatChunk.class);
        } catch (IOException e) {
            throw new GatewayException(502, "bad_upstream_sse",
                    "上游返回了无法解析的 SSE 数据: " + payload);
        }
    }
}
```

说明：

- 用 `BufferedReader` 逐行读取上游 SSE 文本，解析 `data:` 前缀即可；
- `parseSseLine` 返回 `null` 表示忽略该行；JSON 解析失败抛 `GatewayException` 中断整个流；
- 真实上游若带鉴权头，`applyAuth` 会从渠道配置的 `env:` 引用取密钥；
- 实现中已注入 `ObjectMapper`，用于请求序列化与 SSE 解析。

---

## 11. 对外 API：Controller 与统一错误（关键代码段 S9、S10，已实现）

### 11.1 `ChatCompletionController`（代码段 S9）
> 路径：`ai-gateway/src/main/java/com/aigateway/api/controller/ChatCompletionController.java`、`ModelController.java`

```java
@RestController
public class ChatCompletionController {

    private final ChatGatewayService gatewayService;

    @PostMapping("/v1/chat/completions")
    public ResponseEntity<?> chat(@RequestBody ChatRequest request,
                                  HttpServletRequest httpRequest) {
        String requestId = requestId(httpRequest);
        if (request.streaming()) {
            SseEmitter emitter = new SseEmitter(0L);
            Thread.ofVirtual().name("sse-" + requestId).start(() -> {
                try {
                    gatewayService.stream(request, requestId, chunk -> {
                        try {
                            emitter.send(chunk);
                        } catch (IOException e) {
                            throw new UncheckedIOException(e);
                        }
                    });
                    emitter.complete();
                } catch (Exception e) {
                    emitter.completeWithError(e);
                }
            });
            return ResponseEntity.ok()
                    .contentType(MediaType.TEXT_EVENT_STREAM)
                    .body(emitter);
        }
        return ResponseEntity.ok(gatewayService.complete(request, requestId));
    }

    private String requestId(HttpServletRequest httpRequest) {
        Object attr = httpRequest.getAttribute(RequestIdFilter.ATTR);
        return attr != null ? attr.toString() : UUID.randomUUID().toString();
    }
}
```

注：`GET /v1/models` 端点在独立的 `ModelController` 中实现（见第 3 节结构图），本类只负责 `/v1/chat/completions`。

### 11.2 统一错误（代码段 S10）
> 路径：`ai-gateway/src/main/java/com/aigateway/core/exception/GatewayException.java`、`ai-gateway/src/main/java/com/aigateway/api/error/GlobalExceptionHandler.java`

```java
// 路径：ai-gateway/src/main/java/com/aigateway/core/exception/GatewayException.java（已实现）
public class GatewayException extends RuntimeException {
    private final int status;      // HTTP 状态码
    private final String type;     // OpenAI 风格错误类型

    public GatewayException(int status, String type, String message) {
        super(message);
        this.status = status;
        this.type = type;
    }

    public GatewayException(int status, String type, String message, Throwable cause) {
        super(message, cause);
        this.status = status;
        this.type = type;
    }
    // getter 省略
}

// 路径：ai-gateway/src/main/java/com/aigateway/api/error/GlobalExceptionHandler.java（已实现）
@RestControllerAdvice
public class GlobalExceptionHandler {

    public record ApiError(String type, String message) {}

    @ExceptionHandler(GatewayException.class)
    public ResponseEntity<ApiError> handleGateway(GatewayException e) {
        return ResponseEntity.status(e.getStatus())
                .body(new ApiError(e.getType(), e.getMessage()));
    }

    // HttpMessageNotReadableException → 400 invalid_request；Throwable → 500 internal_error
}
```

错误码约定（V1 固定）：

| 场景 | HTTP | type |
|---|---|---|
| 请求体不合法 | 400 | `invalid_request` |
| 模型别名不存在/无可用实例 | 503 | `no_available_model` |
| 所有上游候选失败 | 502 | `upstream_failed` |
| 上游返回错误 | 502 | `upstream_error` |
| 内部错误 | 500 | `internal_error` |

---

## 12. 请求 ID 与指标（关键代码段 S11、S12，已实现）

### 12.1 `RequestIdFilter`（代码段 S11）
> 路径：`ai-gateway/src/main/java/com/aigateway/infra/web/RequestIdFilter.java`

```java
@Component
public class RequestIdFilter implements Filter {
    public static final String ATTR = "requestId";

    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
            throws IOException, ServletException {
        String requestId = UUID.randomUUID().toString();
        request.setAttribute(ATTR, requestId);
        if (response instanceof HttpServletResponse httpResponse) {
            httpResponse.setHeader("X-Request-Id", requestId);
        }
        chain.doFilter(request, response);
    }
}
```

日志约定：GatewayService 中统一用 `log(requestId, event, detail)` 打印 `[requestId=xxx] event=xxx detail=xxx`，方便按 requestId 串起一次调用的完整链路。

### 12.2 `GatewayMetrics`（代码段 S12）
> 路径：`ai-gateway/src/main/java/com/aigateway/observability/GatewayMetrics.java`

```java
@Component
public class GatewayMetrics {
    private final MeterRegistry meterRegistry;

    public void success(String alias, String instanceId) {
        counter("gateway.requests.total", alias, instanceId, "success").increment();
    }

    public void failure(String alias, String instanceId) {
        counter("gateway.requests.total", alias, instanceId, "failure").increment();
    }

    public void streamChunk(String alias, String instanceId) {
        counter("gateway.stream.chunks", alias, instanceId, "").increment();
    }
    // counter(...) 内部用 Counter.builder(name).tag(...).register(meterRegistry)
}
```

访问：`GET /actuator/prometheus`，可以看到 `gateway_requests_total{model="qwen",instance="mock-a:qwen-large",result="success"}` 这类指标。

---

## 13. 健康检查改进（关键代码段 S13）
> 路径：`ai-gateway/src/main/java/com/aigateway/state/health/HealthChecker.java`

> 🖊 **手敲 H4**：仅 `mark()` / `isHealthy()` 手敲；探活调度已实现。

```java
@Component
public class HealthChecker {
    private final GatewayProperties props;
    private final ModelRegistry registry;
    private final HttpClient httpClient;
    private final ObjectMapper objectMapper;

    private final Map<String, Integer> consecutiveFailures = new ConcurrentHashMap<>();
    private final Map<String, Boolean> healthy = new ConcurrentHashMap<>();  // 最新快照

    public void start() {  // @PostConstruct 已实现：定时调度
        Executors.newSingleThreadScheduledExecutor().scheduleAtFixedRate(
                this::checkAll, 0, props.getHealth().getIntervalSeconds(), TimeUnit.SECONDS);
    }

    private void checkOne(String channelId) {
        registry.findChannel(channelId).ifPresent(ch ->
                mark(channelId, probe(ch.baseUrl())));
    }

    /** 阻塞式探活：GET /health，200 且 status=UP 视为健康 */
    private boolean probe(String baseUrl) {
        try {
            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create(baseUrl + "/health"))
                    .timeout(Duration.ofMillis(props.getHealth().getTimeoutMs()))
                    .GET()
                    .build();
            HttpResponse<String> response = httpClient.send(request,
                    HttpResponse.BodyHandlers.ofString());
            if (response.statusCode() != 200) {
                return false;
            }
            Map<?, ?> body = objectMapper.readValue(response.body(), Map.class);
            return "UP".equals(body.get("status"));
        } catch (Exception e) {
            return false;
        }
    }

    // ============================================================
    // TODO H4（手敲）：状态转换
    // mark(channelId, up)：
    //   - up   → 连续失败计数清零，healthy[channelId] = true
    //   - !up  → 连续失败 +1；达到 props.getHealth().getConsecutiveFailures() 才置 false
    // isHealthy(channelId)：
    //   - 读 healthy 快照，默认 true（首次探活前放行）
    // ============================================================
    private void mark(String channelId, boolean up) {
        throw new UnsupportedOperationException(
                "H4 未实现：请手敲 mark() 状态转换逻辑（见详细实施计划第 13 节）");
    }

    public boolean isHealthy(String channelId) {
        throw new UnsupportedOperationException(
                "H4 未实现：请手敲 isHealthy() 快照读取（见详细实施计划第 13 节）");
    }
}
```

注意：

- 健康状态按**渠道**维护（V1 粒度），路由按渠道过滤；
- `isHealthy` 是纯内存读，请求路径零阻塞；
- mock 的 `/health` 返回 `status: DOWN` 时，连续 3 个周期后该渠道被剔除。

---

## 14. mock 服务端修改（关键代码段 S14，已实现）

### 14.1 流式接口改为标准 `POST + SSE`
> 路径：`mock-model-server/src/main/java/com/aigateway/mock/controller/ChatController.java`

```java
@PostMapping(value = "/v1/chat/completions", params = "stream=true")
public SseEmitter chatStream(@RequestBody Map<String, Object> request) {
    ModelProfile profile = registry.getProfile();
    String content = gen.generate(profile);
    if (gen.shouldFail(profile)) {
        throw new RuntimeException("Mock model internal error");
    }
    String[] tokens = splitIntoTokens(content, profile.responseStyle());
    String id = "chatcmpl-" + UUID.randomUUID().toString().substring(0, 8);
    SseEmitter emitter = new SseEmitter(0L);
    Thread.ofVirtual().name("mock-sse-" + id).start(() -> {
        registry.requestStarted();
        try {
            for (String token : tokens) {
                emitter.send(buildChunk(id, profile.name(), token, false));
                Thread.sleep((long) profile.streamingDelayMs());
            }
            emitter.send(buildChunk(id, profile.name(), "", true));
            emitter.complete();
        } catch (Exception e) {
            emitter.completeWithError(e);
        } finally {
            registry.requestFinished();
        }
    });
    return emitter;
}
```

要点：`SseEmitter` 让 Spring 自动按 `data:` 格式输出，网关端用 `BufferedReader` 逐行读取即可。

### 14.2 健康开关（BehaviorController 已支持 health 参数）
> 路径：`mock-model-server/src/main/java/com/aigateway/mock/controller/BehaviorController.java`、`HealthController.java`、`mock/model/ModelRegistry.java`

```java
if (params.containsKey("health")) {
    registry.setHealthy(Boolean.parseBoolean(params.get("health").toString()));
}
```

`HealthController` 已改为：

```java
return Map.of(
    "status", registry.isHealthy() ? "UP" : "DOWN",
    "activeRequests", registry.getCurrentRequests(),
    "queueSize", p.queueSize()
);
```

演示时可以直接 `POST /mock/behavior {"health": false}` 把某实例置为不健康，观察健康检查剔除。

---

## 15. 一次请求的完整时序（非流式）

```
Client
  │ POST /v1/chat/completions {model:"qwen", messages:[...]}
  ▼
RequestIdFilter（生成 requestId，注入 X-Request-Id）
  ▼
ChatCompletionController
  ▼
ChatGatewayService.complete()          ← 🖊 H2
  ├─ ModelRegistry.findByAlias("qwen") → [mock-a 权重8, mock-b 权重2]
  ├─ 过滤：healthChecker.isHealthy(channelId)（mock-b 若 DOWN 则剔除）  ← 🖊 H4
  ├─ WeightedRandomPicker.pick() → 选中 mock-a（约 80%）              ← 🖊 H1
  ▼
OpenAIConnector.complete()             ← 已实现
  └─ JDK HttpClient POST http://localhost:8001/v1/chat/completions
        ▼
     mock ChatController（模拟延迟/故障/生成内容）
  ▼
成功 → ChatCompletion 返回客户端；失败 → 换 mock-b 再试一次（H2）
  ▼
GatewayMetrics 计数 → 结构化日志（requestId 贯穿）
```

流式时序相同，区别是 Connector 通过回调逐 chunk 输出（H3），Controller 以 SseEmitter 分片转发。

---

## 16. 测试计划

### 16.1 单元测试（JUnit + Mockito）

| 测试类 | 用例 | 断言要点 | 对应 |
|---|---|---|---|
| `ai-gateway/src/test/java/com/aigateway/core/service/WeightedRandomPickerTest.java` | 权重 8:2 跑 1000 次 | 分布落在 75%~85% 之间（宽松断言避免 flaky） | H1 |
| `ai-gateway/src/test/java/com/aigateway/core/service/WeightedRandomPickerTest.java` | 单个候选 | 永远返回该候选 | H1 |
| `ai-gateway/src/test/java/com/aigateway/core/service/WeightedRandomPickerTest.java` | 空列表 | `isEmpty()` 为 true，`pick()` 为空 | H1 |
| `ai-gateway/src/test/java/com/aigateway/state/registry/ModelRegistryTest.java` | 引用不存在渠道 | 启动抛 `GatewayException(invalid_config)` | H5 |
| `ai-gateway/src/test/java/com/aigateway/state/registry/ModelRegistryTest.java` | 空候选/重复别名 | 启动失败，错误信息可读 | H5 |
| `ai-gateway/src/test/java/com/aigateway/core/service/ChatGatewayServiceTest.java` | 第一候选失败 → 第二候选成功 | 最终响应来自第二候选，指标记录一次失败一次成功 | H2 |
| `ai-gateway/src/test/java/com/aigateway/core/service/ChatGatewayServiceTest.java` | 全部候选失败 | 返回 502 `upstream_failed` | H2 |
| `ai-gateway/src/test/java/com/aigateway/core/service/ChatGatewayServiceTest.java` | 候选全被健康过滤 | 返回 503 `no_available_model` | H2/H4 |
| `ai-gateway/src/test/java/com/aigateway/execution/connector/OpenAIConnectorTest.java` | SSE 文本解析 | `data: {...}` 解析为 ChatChunk；`[DONE]` 结束；垃圾行忽略 | H3 |
| `ai-gateway/src/test/java/com/aigateway/infra/web/RequestIdFilterTest.java` | 两次请求 | 每个请求有独立 requestId | 脚手架 |

关键测试示例（代码段 S15，H1 完成后可跑）：

```java
// 路径：ai-gateway/src/test/java/com/aigateway/core/service/WeightedRandomPickerTest.java
class WeightedRandomPickerTest {
    @Test
    void weightedDistribution_shouldFollowWeights() {
        var a = instance("a", 8);
        var b = instance("b", 2);
        var picker = new WeightedRandomPicker(List.of(a, b));

        int countA = 0;
        for (int i = 0; i < 1000; i++) {
            if (picker.pick().orElseThrow().instanceId().equals("a")) countA++;
        }
        assertThat(countA).isBetween(750, 850);   // 8/10 概率，宽松区间
    }

    @Test
    void emptyPicker_shouldReturnEmpty() {
        var picker = new WeightedRandomPicker(List.of());
        assertThat(picker.isEmpty()).isTrue();
        assertThat(picker.pick()).isEmpty();
    }
}
```

### 16.2 集成测试

`ai-gateway/src/test/java/com/aigateway/ChatGatewayIntegrationTest.java`：启动真实网关（随机端口）+ 两个 mock 实例，用 `MockMvc` 验证：

1. 非流式返回 OpenAI 格式；
2. `stream=true` 返回 SSE 且最后一个 chunk 的 `finish_reason=stop`；
3. 把 mock-a 故障率调 100% 后，请求仍成功（降级到 mock-b）；
4. 把 mock-b 健康置 DOWN 后，`/v1/models` 仍返回但路由只走 mock-a。

---

## 17. 演示与验收（curl 命令）

```bash
# 1) 启动：mock 两个实例 + 网关
cd mock-model-server && mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8001,--mock.model=qwen-large &
cd mock-model-server && mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8002,--mock.model=qwen-small &
cd ai-gateway && mvn spring-boot:run

# 2) 非流式
curl -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}]}'

# 3) 流式
curl -N -s http://localhost:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen","messages":[{"role":"user","content":"你好"}],"stream":true}'

# 4) 故障注入：mock-a 100% 失败 → 观察自动切换
curl -s -X POST http://localhost:8001/mock/behavior \
  -H 'Content-Type: application/json' -d '{"errorRate":1.0}'

# 5) 健康剔除：mock-b 置为不健康 → 等一个检查周期后流量只走 mock-a
curl -s -X POST http://localhost:8002/mock/behavior \
  -H 'Content-Type: application/json' -d '{"health":false}'

# 6) 指标
curl -s http://localhost:8080/actuator/prometheus | grep gateway_
```

验收标准对照（与学习版文档一致）：

1. 一条命令启动全部服务；
2. 非流式/流式均返回正确 OpenAI 格式；
3. 加权比例接近配置（10% 以内波动）；
4. 故障切换、健康剔除可稳定复现；
5. 核心算法（加权随机/健康过滤/失败切换）有单测覆盖。

---

## 18. 任务清单（按顺序执行）

| # | 任务 | 产出 | 验收 | 归属 |
|---|---|---|---|---|
| T1 | 加依赖、建 DTO 与领域模型 | `ai-gateway/src/main/java/com/aigateway/api/dto/`（ChatRequest/ChatCompletion/ChatChunk）+ `ai-gateway/src/main/java/com/aigateway/core/domain/model/`（Channel/ModelInstance） | 编译通过 | Codex 已做 |
| T2 | 配置绑定与启动校验 | `ai-gateway/src/main/java/com/aigateway/infra/config/GatewayProperties.java`、`SecretResolver.java` + `ai-gateway/src/main/java/com/aigateway/state/registry/ModelRegistry.java` 校验 | 非法配置启动失败且报错可读 | 校验部分 🖊 H5 |
| T3 | 加权随机 | `ai-gateway/src/main/java/com/aigateway/core/service/WeightedRandomPicker.java` + 单测 | 边界用例全绿 | 🖊 H1 |
| T4 | Connector 重构 | `ai-gateway/src/main/java/com/aigateway/execution/connector/OpenAIConnector.java` + 单测 | mock 直接调用两种模式均正常 | 流式部分 🖊 H3 |
| T5 | 主流程 | `ai-gateway/src/main/java/com/aigateway/core/service/ChatGatewayService.java` + 单测 | 故障切换/503/502 用例通过 | 🖊 H2 |
| T6 | Controller 与错误处理 | `ai-gateway/src/main/java/com/aigateway/api/controller/ChatCompletionController.java`、`ModelController.java` + `api/error/GlobalExceptionHandler.java` | curl 演示通过 | Codex 已做 |
| T7 | 请求 ID 与日志 | `ai-gateway/src/main/java/com/aigateway/infra/web/RequestIdFilter.java` + 日志约定 | 日志可按 requestId 串联 | Codex 已做 |
| T8 | 指标 | `ai-gateway/src/main/java/com/aigateway/observability/GatewayMetrics.java` + actuator | `/actuator/prometheus` 有指标 | Codex 已做 |
| T9 | 健康检查改进 | `ai-gateway/src/main/java/com/aigateway/state/health/HealthChecker.java` + 单测 | 置 DOWN 后 3 个周期被剔除 | 状态转换 🖊 H4 |
| T10 | mock 重构 | `mock-model-server/src/main/java/com/aigateway/mock/controller/ChatController.java` 等 mock 类 | 网关流式/健康演示通过 | Codex 已做 |
| T11 | 集成测试 | `ai-gateway/src/test/java/com/aigateway/ChatGatewayIntegrationTest.java` | 4 个场景全过 | 你（H1~H5 完成后） |
| T12 | 文档与演示 | 更新 README / 录演示命令 | 按第 17 节可完整走一遍 | 你 |

---

## 19. 风险与注意点

1. **SSE 帧格式**：`SseEmitter` 输出与上游 `data:` 行解析都要先跑通再写业务逻辑；建议 H3 先做“网关 → mock 流式”的最小联调。
2. **流式读取**：JDK HttpClient + `BufferedReader` 逐行读 SSE 并即时转发；不要先攒完整响应再转发（会失去流式意义）。
3. **健康检查调度**：探活是阻塞调用，跑在单线程 `ScheduledExecutorService` 上（V1 渠道少没问题；渠道多时保证 渠道数 × timeoutMs 不超过 intervalSeconds）；`consecutiveFailures`/`healthy` 用 `ConcurrentHashMap`，路由线程读快照零阻塞。
4. **Map 出入参的坑**：DTO record 默认忽略未知字段，无需额外配置；但新增字段时记得加 `@JsonProperty`（snake_case）。
5. **mock 多实例**：两个 mock 用不同端口启动，用 `--mock.model` 参数区分 `ModelProfile`，避免配置互相覆盖。
6. **手敲顺序建议**：H1 → H5 → H4 → H3 → H2。H1 是纯算法先建立信心；H5/H4 是状态与校验；H3 涉及网络解析；H2 最后串联全部。
