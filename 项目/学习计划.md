# 秋招面试冲刺周计划（细颗粒度版，2026-08-10 ~ 2026-08-16）

> 目标：一周内完全掌握 Agent 项目；完成 Gateway 核心版本并大致掌握。
> 本版把每天「上午 / 下午 / 晚上」细化到：具体看哪些代码文件、掌握哪些重点逻辑、背哪些面试知识点、如何验收。
> 用法：每完成一项就勾选；卡壳的知识点记入文末「薄弱点清单」，周末模拟面试前清空。

## 〇、路径约定

- Agent 代码统一省略前缀 `bibo-agent/src/main/java/com/yourorg/biboagent/`，计划里写 `包/类.java`。
- Gateway 代码统一省略前缀 `ai-gateway/src/main/java/com/aigateway/`，计划里写 `包/类.java`。
- 文档路径从各自项目根目录写全。

## 一、现状盘点（2026-08-10）

- Agent：已完成，`docs/interview/` 有总览 + 10 个模块笔记 + 43 题问题统计；核心代码手敲过一遍 → 本周用「闭卷重建 → 对照 → 出声讲」，不重复手敲。
- Gateway：V1 已完成；V2 H1~H5 基本落地，H6 未接完且当前编译不过（`core/service/ChatGatewayService.java` 146/147 行引用已删除的 registry/healthChecker）；V3 只有实施计划文档，代码未开始；V4~V6 只有学习版文档。
- 旧版《学习计划.md》（从 0 手敲 8 次迭代）已备份为 `docs/学习计划-从零手敲（备份）.md`。

## 二、本周范围与优先级

| 优先级 | 内容 | 说明 |
|---|---|---|
| 必达 | Agent 完全掌握 | 每天上午固定推进 |
| 必达 | Gateway V2 收尾 + V3 全部（H1~H7） | 每天下午固定推进 |
| 弹性 | Gateway V4 起步 | 视 V3 完成情况决定 |
| 放下周 | V4 收尾、V5 热更新、V6 进阶（可选） | 不为「做完」牺牲 Agent |

## 三、学习方法

1. Agent：先闭卷画图 / 默写 → 对照笔记与源码修正 → 出声讲一遍。
2. Gateway：让 Codex 生成脚手架 → 自己手敲 H 清单 → 编译 → 测试 → 演示，当天闭环。
3. 晚上必须复盘：白天内容出声复述 + 抽答问题统计 + 写一页笔记。
4. 每天结束时对照「当日验收」，不达标标 ⚠️ 并记入薄弱点清单。

## 四、掌握验收标准

### Agent

- [ ] 闭卷画出整体架构图和一次完整对话时序
- [ ] 默写 TurnLoop 核心 while 循环
- [ ] 讲清工具调用 6 层链路、记忆 L0/L1/L2、取消穿透与快照恢复
- [ ] `docs/interview/问题统计.md` 抽查 80% 以上不卡壳
- [ ] 完成两轮完整模拟面试（录音回听）

### Gateway

- [ ] V2/V3 编译、测试、演示全部通过
- [ ] 默写 / 口述 V2 H1~H6 与 V3 H1~H7 的核心逻辑
- [ ] 闭卷画出「决策流水线」和「容错状态机」两张图
- [ ] 答出高频题：插件 vs if-else、fail-closed、EWMA、熔断半开、流式为什么不重试、限流与配额区别

---

## 五、每日计划（勾选框）

### 周日 8/9（可选预习，未做可在本周碎片时间补）

- [ ] 通读 `docs/interview/总览.md`
- [ ] 修 Gateway 编译错误（`core/service/ChatGatewayService.java` 146/147 行）
- [ ] 跑 `mvn test` 确认 V2 可编译

### 周一 8/10

#### 上午（Agent · 总览 + 运行时）

目标：闭卷画出整体架构，走通一次完整对话。

代码清单：
- [ ] `api/api/StreamController.java`：双流机制（POST 提交 → requestId → GET 订阅 SSE）、30s 心跳、断连触发 canceller.cancel()
- [ ] `runtime/app/AgentService.java`：8 步总流程（消息预处理 → Prompt 组装 → 历史加载 → 截断 → Turn Loop → 收尾 → 记忆提取 → FINAL）
- [ ] `runtime/app/TurnLoopCoordinator.java`：核心 while 循环 6 步（取消/超时检查 → LLM 调用 → 快照 → 结果解析 → 工具/文本处理 → 消息回写）
- [ ] `runtime/app/PromptAssembler.java`：System Prompt 五段（人设 → Skill → L0 规则 → 记忆 → 娱乐）
- [ ] `runtime/app/SessionLifecycleService.java` + `runtime/infra/RedisSnapshotStore.java`：快照保存 / 恢复 / 清理
- [ ] `runtime/app/SummaryGenerator.java`：不同终止原因的摘要策略
- [ ] `runtime/app/StreamingResponseHandler.java` + `runtime/infra/RedisStreamEventStore.java`：事件双写与重放（主线即可）
- [ ] `runtime/domain/TurnSnapshot.java`：快照字段（为什么没有 terminationSummary）

重点逻辑：
- [ ] 完整对话时序：POST → requestId → 虚拟线程 → 上下文准备 → TurnLoop 循环 → 收尾持久化 → FINAL
- [ ] 三层终止条件（maxSteps=8 / maxToolCalls=4 / totalTimeoutMs=120s）+ CANCELLED / SELF_CORRECT_FALLBACK
- [ ] 快照时机：“LLM 返回后 + 工具执行后保存；正常完成清理；LLM_FAILURE / CANCELLED 保留”
- [ ] 事件双写：Redis（可重放）+ SseEmitter（实时），Last-Event-Id 断点续传

面试知识点：
- [ ] 为什么同步主体 + 虚拟线程而不是 WebFlux？
- [ ] 崩溃最多丢几个 step？为什么？
- [ ] STEP_LIMIT 覆盖 COMPLETED、SELF_CORRECT_FALLBACK 不覆盖——为什么这样设计？
- [ ] SSE 心跳为什么必要？事件为什么要双写？

当日验收：
- [ ] 不看文档画出架构图并默讲 3 分钟
- [ ] 能完整讲出 AgentService 8 步 + TurnLoop 6 步

#### 下午（Gateway · V1 快复盘 + V2 收尾）

目标：V1 概念不欠债；修好编译、完成 H6、演示全绿。

V1 快复盘（30 分钟）：
- [x] `api/controller/ChatCompletionController.java`：非流式 / 流式分流、SseEmitter 裸返回
- [x] `core/service/ChatGatewayService.java`：V1 主流程（健康过滤 → 加权随机 → 失败切换）
- [x] `core/service/WeightedRandomPicker.java`：转盘算法
- [x] `state/health/HealthChecker.java` + `state/registry/ModelRegistry.java`：健康快照、别名 → 候选
- [x] `execution/connector/OpenAIConnector.java`：非流式 / 流式转发、SSE 解析
- [x] `infra/web/RequestIdFilter.java` + `observability/GatewayMetrics.java`：链路 ID 与指标
- [x] mock 三个类：`mock-model-server/.../controller/ChatController.java`、`BehaviorController.java`、`HealthController.java`：延迟 / 故障 / 健康注入

V2 收尾（H6）：
- [x] 修编译：`core/service/ChatGatewayService.java` 146/147 行——删掉 V1 兜底路径对 registry/healthChecker 的引用，改为 DecisionEngine 出方案
- [x] 对照 `Gateway/docs/implementation/版本2-详细实施计划.md` S11：
  - [ ] `decision/engine/DecisionEngine.java`：插件 → 过滤 → 打分 → 调度 → 产出 RoutingDecision
  - [ ] `core/service/ChatGatewayService.java`：主选 + 最小降级执行
  - [ ] `api/controller/ChatCompletionController.java`：metadata(tenant/canary_group) 传入 service
- [x] `mvn test` 全绿
- [x] 演示 A/B/C：复杂度识别（质量优先）、金丝雀分组（只进稳定候选）、fail-closed（候选清空 → 拒绝）
- [x] V1 回归：非流式 / 流式 / 健康过滤 / 失败切换

重点逻辑：
- [x] 决策与执行分离：决策层产出不可变 RoutingDecision，执行层只消费
- [x] 插件只收窄候选、fail-closed 不回退全量
- [x] FilterChain 五道关卡顺序：能力 → 可用性 → 策略 → 预算 → 插件信号
- [x] Scorer：归一化 → 加权 → 动态调权（task_complexity / budget 两条规则）
- [x] EWMA：`ewma_new = α × 本次延迟 + (1 - α) × ewma_old`

面试知识点：
- [x] 插件 vs if-else 的本质区别
- [ ] fail-closed 为什么宁可拒绝也不能回退
- [ ] 为什么打分明细 = 可解释路由
- [ ] 多目标 / 加权随机 / 条件路由三种策略各自适用场景

当日验收：
- [ ] `mvn test` 全绿；三条演示日志能解释「为什么选它」

#### 晚上（复盘）

- [ ] 用自己的话复述 `docs/interview/10-简历项目详解.md` 第一节（录音 5 分钟）
- [ ] 抽答 `docs/interview/问题统计.md` 第 1~10 题，答不出的标红
- [ ] 写 V2 一页笔记：插件化 / fail-closed / 打分 / 调度 / EWMA 五个点
- [ ] 卡壳点记入薄弱点清单

### 周二 8/11

#### 上午（Agent · LLM + 工具系统）

目标：讲清 LLM 异步化与工具 6 层执行链路。

代码清单（llm）：
- [x] `llm/infra/LlmInvoker.java`：Mono.create 桥接、AtomicBoolean CAS 竞态裁决、retryWhen 退避
- [x] `llm/infra/ChatRequestBuilder.java`：消息 + 工具 → ChatRequest；为什么每次 new ArrayList 副本
- [x] `llm/infra/LlmClientFactoryImpl.java`：按 ModelSpec 创建 StreamingChatModel
- [x] `llm/infra/SyncLlmInvoker.java`：CountDownLatch 同步封装（对照理解异步化的价值）
- [x] `llm/infra/LlmRequestAdapter.java` + `llm/infra/ModelNameResolver.java`：厂商参数适配
- [x] `modelregistry/infra/YamlModelRegistry.java`：enabled / disabled / defaultModel
- [x] `runtime/app/StreamingResponseHandler.java`：onPartialResponse / onCompleteToolCall / onCompleteResponse / onError

代码清单（tools，按调用链顺序）：
- [x] `runtime/app/PlanExecutor.java`：工具统一入口，三条路径（显式 Plan / Interactive 恢复 / 隐式单 Stage）
- [x] `runtime/app/StageScheduler.java`：Stage 串行 + Stage 内并行、占位符解析（先看主流程）
- [ ] `runtime/app/SingleToolExecutor.java`：取消检查 → 风险鉴权 → 超时预算 → 执行 → 归一化 → 源头限长
- [ ] `tools/infra/RetryableToolExecutor.java`：幂等缓存 → 重试循环 → 结果缓存
- [ ] `tools/infra/ToolIdempotencyCache.java`：Redis 主 + 本地备用双写
- [ ] `tools/infra/DefaultToolExecutor.java` + `tools/infra/ToolInvoker.java`：查找 @Tool → Schema 校验 → 参数反序列化 → 反射调用
- [ ] `tools/infra/ToolMetadataRegistry.java` + `tools/infra/ToolParamValidator.java`：注册中心与两层校验
- [ ] `tools/infra/DefaultToolResultNormalizer.java` + `tools/infra/DefaultToolPolicyService.java`：归一化与风险策略
- [ ] `runtime/app/FinishToolSchema.java`：finish 虚拟终止信号
- [ ] `runtime/app/RecallToolResultTool.java` + `runtime/infra/ToolResultArchive.java`：结果归档与追问（读主线）

重点逻辑：
- [ ] 工具调用完整链路 6 层：StageScheduler → SingleToolExecutor → RetryableToolExecutor → DefaultToolExecutor → ToolInvoker → @Tool 方法
- [ ] 错误分类表：SUCCESS / SCHEMA_VALIDATION_FAILED / TOOL_EXEC_ERROR / RETRYABLE / TIMEOUT / FORBIDDEN / UNKNOWN_TOOL / CANCELLED
- [ ] 重试三条件：工具可重试 + 错误可重试 + 未达最大次数；线性退避
- [ ] 幂等键生命周期：创建 → 缓存检查 → 重试 → 结果存储
- [ ] finish 工具为什么不进注册表、检测后直接 break、COMPLETED 时过滤掉
- [ ] 工具结果按 toolCallId 精确回注（同轮同名工具不串结果）

面试知识点：
- [ ] Mono 是什么？为什么只在 LLM 调用和工具执行两个阻塞点用？
- [ ] 首 token 前超时重试、首 token 后不重试的原因
- [ ] @Tool 扫描的坑（getBeanNamesForAnnotation 只认类注解）与双保险
- [ ] 幂等缓存 Redis 挂了怎么降级

当日验收：
- [ ] 默写 TurnLoop 循环骨架；画出工具 6 层并讲清每层职责

#### 下午（Gateway · V3 启动 + H1/H2）

目标：建立 V3 容错心智模型，手敲重试与分层超时。

先读（不是代码）：
- [x] `Gateway/docs/plan/版本3-容错与流式治理.md` 学习版 4.1~4.7 与第 7 节 Q1~Q4
- [x] `Gateway/docs/implementation/版本3-详细实施计划.md` 第 0~7 节（分工、失败分类、配置）

脚手架准备：
- [x] 让 Codex 生成 V3 脚手架（第 0 节列出的配置绑定 / Failure / FailureType / UpstreamCallException / FailureClassifier / ExecutionChainResolver 等）
- [x] 读懂脚手架：失败分类怎么映射（连接失败 / 超时 / 429 / 5xx / 4xx）

手敲：
- [x] 🖊 H1 `execution/retry/RetryExecutor.java`：触发条件 / 指数退避 + 抖动 / Retry-After / 首字节安全边界
- [x] 🖊 H2 `execution/timeout/TimeoutGuard.java`：连接 / 完整响应 / 首字节 / 流式空闲 / 整链总时长
- [ ] 写单测：`RetryExecutorTest`、`TimeoutGuardTest`（对照实施计划示例）

重点逻辑：
- [ ] 什么失败能重试：连接失败、超时、429、5xx；4xx 不重试
- [ ] 重试优先换候选；同实例只在连接级失败时重试一次
- [ ] 流式安全边界：首字节前可重试，首字节后只能降级 / 报错
- [ ] 分层超时为什么必要：精确回答“卡在哪一步”，慢上游不占死线程

面试知识点：
- [ ] 重试 vs 降级；熔断 vs 冷却（今天先记住区别，周三细讲）
- [ ] 流式为什么不能随便重试（客户端已收到半段内容）

当日验收：
- [ ] 能默写 H1/H2 核心；能说出哪类失败可重试、为什么

#### 晚上（复盘）

- [ ] 口述 V3 失败分类（录音 5 分钟）
- [ ] 抽答 `问题统计.md` 第 11~20 题
- [ ] Agent 工具链路再讲一遍（出声）
- [ ] 卡壳点记入薄弱点清单

### 周三 8/12

#### 上午（Agent · 记忆系统）

目标：吃透三级记忆、提取、检索、整理、遗忘。

代码清单：
- [ ] `memory/app/MemoryExtractor.java`：P0 / P1 / P2 三级提取、写入去重、矛盾检测
- [ ] `memory/app/MemoryRetriever.java`：L1 命中 → L2 检索、importance 加权、关系补充
- [ ] `memory/app/MemoryService.java`：用例编排
- [ ] `memory/app/SearchMemoryTool.java` + `memory/app/WriteMemoryTool.java`：LLM 主动读写记忆
- [ ] `memory/infra/PostgresMemoryStore.java`：pgvector SQL、HNSW、检索排序公式
- [ ] `memory/infra/OllamaEmbeddingService.java`：embedding 生成与容错（返回 null 降级）
- [ ] `memory/infra/MemoryConsolidator.java`：每小时相似合并
- [ ] `memory/infra/ForgetScheduler.java`：衰减 / DORMANT / ARCHIVED / 容量控制
- [ ] `memory/infra/ClasspathStaticRuleProvider.java`：L0 静态规则
- [ ] `runtime/app/PromptAssembler.java`：记忆注入 token 预算（只看记忆部分）

重点逻辑：
- [ ] 查询路径：L0 注入 → L1 命中返回 → L1 未命中回源 L2 → 回填 L1
- [ ] 检索公式：`final_score = (1 - cosine_distance) × importance × (emotion_tag 匹配 ? 1.1 : 1.0)`
- [ ] 提取三级互斥：P0 关键词同步 + 即时反馈；P1 step≥2 异步深度；P2 周期 + 信息密度异步浅度
- [ ] 去重：cosine > 0.85 合并全部超阈值记忆（取最大 importance、并集 tags）
- [ ] 矛盾检测：cosine [0.4, 0.85) 且 tags 有交集 → LLM 判断 → importance × 0.7 + CONTRADICTS 关系
- [ ] 遗忘：未访问每日 ×(1-0.02)；0.3/30 天 → DORMANT；0.1/90 天 → ARCHIVED；检索命中递增 importance

面试知识点：
- [ ] 为什么分层记忆（L0/L1/L2）而不是全放 pgvector？
- [ ] cosine vs Jaccard 的取舍
- [ ] L1 为什么分别缓存 embedding 和记忆（省 Ollama 网络 IO vs 省数据库 IO）
- [ ] 记忆注入为什么有 token 预算（默认 800 / 20 条）

当日验收：
- [ ] 默写检索公式；能画出三级提取的 if-else 互斥链

#### 下午（Gateway · V3 H3/H4）

目标：手敲熔断状态机与渠道冷却。

先读：
- [x] `Gateway/docs/implementation/版本3-详细实施计划.md` 第 10、11 节
- [x] 学习版 4.4 / 4.5（熔断与冷却）

手敲：
- [x] 🖊 H3 `execution/circuit/CircuitBreaker.java`：滑动窗口失败率 / 慢调用率 / 半开放行探测
- [x] 🖊 H4 `execution/cooldown/CooldownManager.java`：连续失败计数 → 冷却 → 到期恢复 → 探测失败翻倍
- [x] 写单测：`CircuitBreakerTest`、`CooldownManagerTest`

演示：
- [ ] mock A 故障率 100%：先看到重试 / 降级 → 熔断打开（快速失败）→ 调回 0 → 半开探测成功 → 熔断关闭

重点逻辑：
- [ ] 状态机：关闭（统计）→ 打开（快速失败）→ 半开（放行 1 个）→ 成功关闭 / 失败重开
- [ ] 熔断按「渠道 + 模型」，冷却按「渠道」；两者独立触发，任一命中即剔除
- [ ] 冷却恢复后第一个请求是探测，失败则时长翻倍（有上限）

面试知识点：
- [ ] 熔断 vs 冷却的分工（短时抽风 vs 持续故障）
- [ ] 滑动窗口 vs 从启动累计（旧故障不应永远影响判断）
- [ ] 半开为什么只放行 1 个探测

当日验收：
- [ ] 画出熔断状态机；能讲清熔断与冷却配合

#### 晚上（复盘）

- [ ] 口述熔断状态机 + 冷却翻倍（录音）
- [ ] 抽答 `问题统计.md` 第 21~30 题
- [ ] 记忆系统闭卷讲一遍（出声）
- [ ] 卡壳点记入薄弱点清单

### 周四 8/13

#### 上午（Agent · 子代理 + 容灾 + 上下文压缩）

目标：讲清并行执行、取消穿透、快照恢复与上下文压缩。

代码清单：
- [ ] `subagent/infra/VirtualThreadSubagentRunner.java`：虚拟线程 + watchdog 50ms 轮询 + 批量 cancel(true)
- [ ] `subagent/domain/Canceller.java`：AtomicBoolean 取消协议
- [ ] `runtime/app/StageScheduler.java`：超时均分 perTaskMs、AGGREGATE / DEPENDENT、占位符解析
- [ ] `runtime/app/PlaceholderResolver.java` + `runtime/app/StageConstraintValidator.java`：跨 Stage 依赖与循环校验
- [ ] `runtime/app/PlanParser.java` + `runtime/app/CreatePlanTool.java`：显式计划
- [ ] `runtime/app/HistoryTruncator.java`：token 估算、70% 阈值、6 维结构化摘要、递归兜底
- [ ] `runtime/infra/ToolResultTrimmer.java`：工具结果源头限长
- [ ] `runtime/app/CompressionCache.java`：异步预压缩（读主线，理解版本号 CAS）
- [ ] `runtime/infra/RedisSnapshotStore.java`（复看）：快照恢复

重点逻辑：
- [ ] 取消穿透链路：SSE 断连 → Canceller → TurnLoop → PlanExecutor → StageScheduler → watchdog → future.cancel(true)，延迟 ≤ 50ms
- [ ] AGGREGATE（单任务失败不影响其他） vs DEPENDENT（任一失败短路取消剩余）
- [ ] 快照恢复：每步保存 → 恢复步数与消息 → 正常完成清除；LLM_FAILURE / CANCELLED 保留
- [ ] 压缩：占比 ≥70% 才截断；6 维 JSON 摘要；工具调用与结果成对保留防孤儿工具；递归最多 2 层

面试知识点：
- [ ] 虚拟线程 vs 线程池；为什么并行任务值得 watchdog、单工具不值得
- [ ] 宕机最多丢几步？为什么
- [ ] 截断为什么按轮次而非按消息条数
- [ ] 工具结果为什么要源头限长 + 归档（recall_tool_result）

当日验收：
- [ ] 画出「断连 → 取消 → 快照恢复」时序图；能讲 6 维摘要字段

#### 下午（Gateway · V3 最难点 H5/H6）

目标：手敲降级链与流式治理。

先读：
- [ ] `Gateway/docs/implementation/版本3-详细实施计划.md` 第 12、13 节
- [ ] 学习版 4.3 / 4.6

手敲：
- [ ] 🖊 H5 `execution/fallback/FallbackChainExecutor.java`：按序执行、每候选尝试次数有限、整链总时长、事件记录 from→to
- [ ] 🖊 H6 `execution/stream/StreamProxy.java`：SSE 逐 chunk 规范化、补齐缺失字段、错误事件转统一错误体、取消上游、空闲超时、计量回调
- [ ] 写单测：`FallbackChainExecutorTest`、`StreamProxyTest`

演示：
- [ ] 流式中途失败：客户端收到统一错误事件，不再重放
- [ ] 客户端断连：mock 侧看到连接释放日志，无资源泄漏

重点逻辑：
- [ ] 降级链与重试的配合：重试用尽 → 换下一个候选 → 新候选再走自己的重试
- [ ] StreamProxy 三件事：格式正确（SSE 规范化）、边界安全（首字节后不重试）、资源释放（断开即取消上游）
- [ ] 计量回调失败不影响转发（V4 接入）

面试知识点：
- [ ] 为什么流式不能随便重试（面试必答）
- [ ] 客户端断开怎么做到“通知上游停菜”
- [ ] 空闲超时解决什么问题

当日验收：
- [ ] 演示日志能看到上游取消；能口述 StreamProxy 的 5 个职责

#### 晚上（复盘）

- [ ] 口述「为什么流式不能简单重试」+ V3 前 6 个 H 串讲（录音）
- [ ] 抽答 `问题统计.md` 第 31~43 题
- [ ] Agent 容灾问答（快照 / 取消 / 幂等）
- [ ] 卡壳点记入薄弱点清单

### 周五 8/14

#### 上午（Agent · API / 安全 / 模型注册 / Skill / 可观测）

目标：把外围模块讲成一个整体。

代码清单：
- [ ] `api/api/StreamController.java`（复看）：SSE 事件格式、心跳、Last-Event-Id 重放
- [ ] `api/api/SessionController.java` + `api/api/UserEventController.java`：会话 CRUD、用户级事件流
- [ ] `api/infra/UserEventEmitter.java` + `api/infra/GlobalExceptionHandler.java`：事件持久化、统一异常
- [ ] `security/infra/SecurityConfig.java` + `security/infra/JwtAuthFilter.java`：Filter 顺序、JWT 校验
- [ ] `security/infra/DevModeFilter.java` + `security/infra/SanitizerFilter.java` + `security/infra/SensitiveDataRedactor.java`：开发模式、OWASP 清洗、脱敏
- [ ] `modelregistry/infra/YamlModelRegistry.java` + `modelregistry/api/ModelController.java`：模型选择
- [ ] `skill/app/SlashCommandParser.java` + `skill/infra/SkillFileLoader.java` + `skill/infra/InMemorySkillRegistry.java` + `skill/app/SkillImportService.java`：Skill 加载与热重载
- [ ] `observability/infra/TraceFilter.java` + `observability/infra/MicrometerMetricsPort.java` + `observability/infra/PostgresAuditLogPort.java` + `observability/infra/InfraHealthChecker.java`：追踪 / 指标 / 审计 / 健康

重点逻辑：
- [ ] Filter 顺序：Trace(0) → DevMode(1) → Sanitizer(2) → JwtAuth
- [ ] SSE 双流：POST 提交 → requestId；GET 订阅 → 事件重放（Last-Event-Id）
- [ ] Skill 流程：/code 触发 → SlashCommandParser 抹去指令 → mergeSkillIds → 注入 System Prompt
- [ ] Tool vs Skill：手臂 vs 业务大脑

面试知识点：
- [ ] JWT 认证流程；未配置密钥为什么跳过
- [ ] devMode（X-Dev-UserId）与生产模式的差异
- [ ] 指标清单：LLM 耗时 / 工具重试 / 幂等命中率
- [ ] 审计日志为什么用 JSONB

当日验收：
- [ ] 能闭卷列出 API 分组 + 四道安全防线 + 指标清单

#### 下午（Gateway · V3 收尾 + V2/V3 合并 + 全量验收）

目标：打通状态闭环，合并两版，全量回归。

手敲：
- [ ] 🖊 H7 `decision/state/StateEventPipeline.java` + `decision/state/ModelStateStore.java` 扩展（EWMA 延迟 / 错误率 / 熔断 / 冷却 / 慢调用）
- [ ] 合并 V2/V3：候选链来源改为 `RoutingDecision.primary() + fallbackChain()`（对照实施计划第 2.1 节）
- [ ] 全量 `mvn test`：V2 单测 + V3 单测 + 3 个集成场景 + V1 回归
- [ ] 演示三件套全跑：V2 决策 A/B/C + V3 故障注入 + 流式断连
- [ ] 更新 `README.md` 项目状态与快速开始

重点逻辑：
- [ ] 状态事件管道单写者：执行结果 / 超时 / 熔断转换 / 冷却进出统一写入 → 聚合为 ModelState 快照
- [ ] 决策读最近快照；状态更新频率与决策频率解耦
- [ ] V2/V3 职责边界：V2 只决策、V3 只执行

面试知识点：
- [ ] 为什么用事件管道而不是每个模块直接写 ModelState（并发安全 / 单一事实来源）
- [ ] V2 与 V3 并行开发怎么避免踩踏（候选链来源切换点）

当日验收：
- [ ] 全量测试绿；V2+V3 完整串讲一次（插件 → 过滤 → 打分 → 调度 → 执行 → 重试/超时/熔断/冷却 → 降级 → 流式 → 状态闭环）

#### 晚上（复盘）

- [ ] Gateway 全链路串讲（录音 10 分钟）
- [ ] Agent 全模块快问快答（每个模块 30 秒讲定位）
- [ ] 写 V3 一页笔记：失败分类 / 状态机 / 流式边界 / 状态闭环
- [ ] 卡壳点记入薄弱点清单

### 周六 8/15

#### 上午（Agent · 人格 / 情绪 / 娱乐 / 主动消息）

目标：把“AI 伴侣”场景讲出彩。

代码清单：
- [ ] `persona/app/PersonaService.java` + `persona/app/EmotionTracker.java`：角色 CRUD、VA 二维情绪更新
- [ ] `persona/domain/EmotionState.java`：dominantEmotion 映射
- [ ] `persona/infra/YamlTemplateLoader.java` + `persona/infra/PostgresPersonaRepository.java`：模板与持久化
- [ ] `entertainment/infra/LocalMediaScanner.java`：增量扫描指纹（fileSize:lastModified）
- [ ] `entertainment/infra/MetadataEnrichmentServiceImpl.java`：provider 链（iTunes 封面 / LRCLIB + 网易云歌词）
- [ ] `entertainment/app/PlaybackStateService.java` + `entertainment/infra/RedisPlaybackStateStore.java`：clientTimestamp 防多端覆盖
- [ ] `entertainment/infra/MediaSyncServiceImpl.java` + `entertainment/app/ProactiveMessageGenerator.java`：事件检测与频控
- [ ] `entertainment/infra/tools/PlayMediaTool.java` + `entertainment/infra/tools/SearchMediaTool.java` + `entertainment/infra/tools/ReadBookContentTool.java`：三个代表性工具
- [ ] `proactive/app/ProactiveScheduler.java` + `proactive/infra/CronTriggerService.java`：定时主动消息
- [ ] 前端只定位（不细究）：`ui/src/hooks/useSSE.js`、`ui/src/hooks/useUserSSE.js`、`ui/src/hooks/useMediaPlayer.js`、`ui/src/store/useChatStore.js`

重点逻辑：
- [ ] 情绪更新公式：新状态 = 衰减后状态 × 0.4 + 用户影响 × 0.4；VA 映射 7 种情绪
- [ ] 情绪影响记忆：emotion_tag 匹配 ×1.1
- [ ] 播放控制“AI 大脑 → 浏览器手脚”：后端不播放，只推 PLAYBACK_COMMAND 指令
- [ ] 主动消息频控：按 userId / MediaType 阈值（MIN_INTERVAL_MS=2min）

面试知识点：
- [ ] 为什么情绪用 VA 二维模型而不是直接打标签
- [ ] 为什么播放控制用 SSE 推指令而非后端播放
- [ ] 多标签页播放状态一致性怎么保证
- [ ] Persona vs Skill 的区别

当日验收：
- [ ] 能讲出 persona 模块如何组装 system prompt；能讲一条“放歌”的完整数据流

#### 下午（Gateway · V4 弹性任务）

目标：V4 概念到位；有余力则动手。

必做（概念）：
- [ ] 读 `Gateway/docs/plan/版本4-治理与计量.md` 学习版 4.1~4.5
- [ ] 口述三个区别：令牌（凭证）/ 限流（速度）/ 配额（总量）
- [ ] 口述时序：预检 → 扣减 → 结算；为什么并发下不能大锁
- [ ] 口述流式计量：prompt 预估占位 + completion 增量累计 + 误差标注

选做（如果 V3 已全部完成）：
- [ ] 让 Codex 生成 `Gateway/docs/implementation/版本4-详细实施计划.md`
- [ ] 手敲第一个 H（建议：Token 计量 或 限流滑动窗口）
- [ ] 否则：只完成上面“必做”概念，写下周 V4 清单

当日验收：
- [ ] 能口述令牌 / 限流 / 配额三者区别和预检 → 扣减 → 结算

#### 晚上（复盘）

- [ ] 两个项目交叉随机抽问各 10 题（可让 Codex 出题）
- [ ] 整理本周薄弱点清单
- [ ] 把 Agent 两个最不熟的点重新闭卷画一遍

### 周日 8/16

#### 上午（Agent · 模拟面试 #1）

- [ ] 按 `docs/interview/10-简历项目详解.md` 当脚本：项目介绍 3 分钟 → 架构 5 分钟 → 深挖 20 分钟（Runtime / 工具 / 记忆 / 容灾）
- [ ] 全程录音，不回看文档
- [ ] 回听录音，对照 `docs/interview/问题统计.md` 查漏，把卡壳题标红
- [ ] 补一个最弱模块的闭卷重画

#### 下午（Gateway · 总验收 + 补短板）

- [ ] V2/V3 全部演示 + 测试再跑一遍
- [ ] 把 V2/V3 笔记合并成「Gateway 一页纸」：决策流水线 + 容错状态机两张图
- [ ] 用一页纸自讲 10 分钟（录音）
- [ ] 列出 V4 下周清单（哪些 H 需要 Codex 先生成实施计划）

#### 晚上（复盘 + 下周计划）

- [ ] 对照第四节验收标准逐条打勾
- [ ] 未达标项写入「下周必补」
- [ ] 写下周安排：Agent 二轮模拟 + Gateway V4/V5
- [ ] 早睡，保持状态

---

## 六、下周展望（8/17 起）

- Agent：第二轮模拟面试 + 薄弱点专项（按问题统计中标红的问题）
- Gateway：V4 收尾（令牌 / 限流 / 配额 / Token 计量）、V5 配置热更新与管理面、V6 选 1~2 个进阶能力

## 七、薄弱点清单（每天填）

- [ ] （例：8/11 答不出的问题：……）
